# Hybrid Mode Environment Variables
# Copy this to your .env file when running hybrid K8s deployment

# Price Service Routes to K8s NodePort (port 30083)
PRICE_SERVICE_URI=http://host.docker.internal:30083
PRICE_SERVICE_WS_URI=ws://host.docker.internal:30083

# All other variables same as .env
# This file only contains the overrides needed for hybrid mode
